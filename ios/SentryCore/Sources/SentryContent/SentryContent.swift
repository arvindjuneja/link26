import Foundation

/// The resource bundle holding the GENERATED `content.json`, `copy.json` and
/// `daily.json`. Written by `npm run soc:export` (C1); never hand-edited.
public enum SentryContent {
  public static let bundle = Bundle.module
}
